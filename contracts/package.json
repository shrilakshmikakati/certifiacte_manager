{
  "name": "certificate-manager-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for decentralized certificate management on Ganache",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-ganache.js --network ganache",
    "deploy:ganache": "hardhat run scripts/deploy-ganache.js --network ganache",
    "deploy:localhost": "hardhat run scripts/deploy-ganache.js --network localhost",
    "test": "hardhat test",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "ganache:start": "ganache --port 7545 --accounts 10 --deterministic",
    "ganache:deploy": "npm run deploy:ganache"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.6",
    "@types/mocha": "^10.0.2",
    "@types/node": "^20.6.3",
    "chai": "^4.3.8",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "keywords": [
    "blockchain",
    "certificates", 
    "ganache",
    "ethereum",
    "smart-contracts"
  ],
  "author": "Certificate Manager Team",
  "license": "MIT"
}
