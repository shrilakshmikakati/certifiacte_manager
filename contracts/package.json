{
  "name": "certificate-manager-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for decentralized certificate management on zkSync Era",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-ganache.js --network ganache",
    "deploy:ganache": "hardhat run scripts/deploy-ganache.js --network ganache",
    "deploy:localhost": "hardhat run scripts/deploy-hardhat.js --network localhost",
    "deploy:hardhat": "hardhat run scripts/deploy-hardhat.js --network hardhat",
    "test": "hardhat test",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "ganache:start": "ganache --host 0.0.0.0 --port 7545 --accounts 10 --deterministic",
    "ganache:deploy": "npm run deploy:ganache"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^17.2.3",
    "hardhat": "^2.19.0"
  },
  "devDependencies": {
    "@matterlabs/hardhat-zksync-chai-matchers": "^0.1.4",
    "@matterlabs/hardhat-zksync-node": "^0.1.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@types/chai": "^4.3.6",
    "@types/mocha": "^10.0.2",
    "chai": "^4.3.8",
    "hardhat": "^2.17.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "keywords": [
    "blockchain",
    "certificates",
    "zksync",
    "ethereum",
    "smart-contracts"
  ],
  "author": "Certificate Manager Team",
  "license": "MIT"
}
